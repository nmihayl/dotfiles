# yazi
alias c="yazi"
alias cy="y"

# eza
alias l="eza -1 -G --hyperlink --icons=always --group-directories-first -a"
alias ld="eza -1 -l --hyperlink --icons=always -a -D"
alias ldg="eza -1 -G --hyperlink --icons=always -a -D"
alias ls="eza -1 -l --hyperlink --icons=always --group-directories-first -a"
alias lsgs="eza -1 -G --hyperlink --icons=always --group-directories-first -a -s=size"
alias lss="eza -1 -l --hyperlink --icons=always --group-directories-first -a -s=size"

# git
alias gs="git status"
alias gc="git commit"
alias gps="git push"
alias gpl="git pull"
alias ga="git add"
alias gr="git rm"

# mpv
alias apv="mpv --no-video"

# du
alias ssortr="du -h --max-depth=1 | sort -h"
alias ssort="du -h --max-depth=1 | sort -hr"

# others
alias cd-log="cd /mnt/seagate2tb/Backlog"
alias clean-folders="find . -type d -empty -delete"
alias ipv4="curl -s https://checkip.amazonaws.com"
alias fetch="fastfetch"
